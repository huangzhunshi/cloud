eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
server:
  port: 9101

endpoints:
  shutdown:
    sensitive: false
    enabled: true
# curl -X POST http://127.0.0.1:9101/shutdown  服务下线命令

spring:
  application:
    name: service-hi-a